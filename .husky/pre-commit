#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

echo "🔍 Running pre-commit checks..."
echo "Running lint-staged (ESLint + Prettier)..."

npx lint-staged && echo "✨ Code formatting and linting passed!"